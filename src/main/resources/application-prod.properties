# Production Profile - Railway PostgreSQL Configuration

# PostgreSQL Database Configuration (Railway will provide DATABASE_URL)
spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver

# PostgreSQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Email Configuration (Production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:ritwiksingh0987@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:nkdc hmjj zznr ioih}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# DevTools disabled in production
spring.devtools.restart.enabled=false

# Thymeleaf cache enabled in production for better performance
spring.thymeleaf.cache=true
